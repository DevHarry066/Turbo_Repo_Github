import { Button } from "@repo/ui/button";

export default function App() {
    return <div>
        Hi from admin page.tsx in docs
        <Button appName="React dev" >React Button</Button>
    </div>
}